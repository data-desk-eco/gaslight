[project]
name = "gaslight"
version = "0.1.0"
description = "Dark flaring analysis for the Permian Basin"
requires-python = ">=3.10"
dependencies = [
    "playwright",
    "requests",
    "beautifulsoup4",
    "python-dotenv",
    "lxml>=6.0.2",
    "httpx",
]
